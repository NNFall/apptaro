services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - /root/taro_bot/data:/app/data
      - /root/taro_bot/media:/app/media
    environment:
      DATABASE_PATH: /app/data/database.db
      MEDIA_TEMP_DIR: /app/media/temp
      FFMPEG_PATH: ffmpeg
