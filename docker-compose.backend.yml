services:
  pmapptaro_backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pmapptaro_backend
    restart: unless-stopped
    env_file:
      - ./.env
    ports:
      - "${HOST_PORT:-8022}:8000"
    volumes:
      - ./data:/data
      - ./templates:/app/runtime/templates
      - ./tarot:/app/runtime/tarot
      - ./temp:/app/runtime/temp
      - ./logs:/app/logs

  pmapptaro_admin_bot:
    build:
      context: .
      dockerfile: ./telegram_admin_bot/Dockerfile
    container_name: pmapptaro_admin_bot
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      ADMIN_DATABASE_PATH: /data/pmapptaro.db
      ADMIN_BOT_HEARTBEAT_PATH: /tmp/admin_bot.heartbeat
    volumes:
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "test -f /tmp/admin_bot.heartbeat && [ $$(($$(date +%s)-$$(stat -c %Y /tmp/admin_bot.heartbeat))) -lt 180 ]"]
      interval: 60s
      timeout: 10s
      retries: 2
      start_period: 90s
